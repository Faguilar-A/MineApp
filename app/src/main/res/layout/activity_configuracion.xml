<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/minecraft"
    tools:context=".ConfiguracionActivity">

    <!-- TOOLBAR CON BOTÓN ATRÁS -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@drawable/minecraft2"
            android:title="Configuración"
            android:titleTextColor="@android:color/white"
            android:titleTextAppearance="@style/ToolbarTitleStyle"
            app:navigationIconTint="@android:color/white"
            app:titleCentered="true" />
    </com.google.android.material.appbar.AppBarLayout>

    <!-- CONTENIDO PRINCIPAL -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:gravity="center">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Configuración del Dispositivo"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="@android:color/white"
                android:fontFamily="@font/minecraft"
                android:layout_marginBottom="32dp" />

            <Button
                android:id="@+id/btnWifiSettings"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Abrir Configuración Wi-Fi"
                android:textSize="18sp"
                android:layout_marginBottom="16dp"
                android:padding="16dp"
                android:fontFamily="@font/minecraft" />

            <Button
                android:id="@+id/btnBluetoothSettings"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Abrir Configuración Bluetooth"
                android:textSize="18sp"
                android:layout_marginBottom="16dp"
                android:padding="16dp"
                android:fontFamily="@font/minecraft" />

            <Button
                android:id="@+id/btnWirelessSettings"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Configuración Inalámbrica"
                android:textSize="18sp"
                android:padding="16dp"
                android:fontFamily="@font/minecraft" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>